# AGENTS — savr-platform

Rules specific to the `savr-platform/` directory.

---

- **Do not initialize a framework yet.** The framework choice depends on what is discovered in `SAVR-old/`.
- **Do not create package files yet.** No `package.json`, `pyproject.toml`, `go.mod`, or equivalent.
- **Do not choose a package manager yet.** npm, pnpm, Yarn, pip, etc. — all deferred until after discovery.
- **Do not create a frontend, backend, database, or deployment configuration yet.**
- **The initial contents of this directory will be determined from `SAVR-old/`.** Wait for the architecture-discovery task before creating any files here.

---

This directory will remain empty until `SAVR-old/` is imported and inspected.
